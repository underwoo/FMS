name: GitHub on Self-hosted Runner Test

on: [push, pull_request]

jobs:
  test:
    runs-on: [self-hosted]
    steps:
    - name: Checkout code
      uses: actions/checkout@v2 
    - name: simple commands
      run: |
        hostname
        pwd
        ls

